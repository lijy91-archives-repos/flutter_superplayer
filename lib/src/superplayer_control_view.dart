import 'package:flutter/material.dart';

const kControlViewTypeDefault = 'default';
const kControlViewTypeWithout = 'without';

class SuperPlayerControlView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
